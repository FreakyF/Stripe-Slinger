fmt:
  extends: .rust-changes
  stage: lint
  script:
    - cargo fmt --all -- --check

clippy:
  extends: .rust-changes
  stage: lint
  script:
    - cargo clippy --workspace --all-targets --all-features -- -D warnings
