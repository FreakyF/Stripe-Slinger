fmt:
  stage: lint
  script:
    - cargo +nightly fmt --all -- --check

clippy:
  stage: lint
  script:
    - RUSTFLAGS="-A dead_code -A unused_variables -A unused_mut" \
      cargo +nightly clippy --workspace --all-targets --all-features
